# 非敏感运行配置。密码等密钥必须放在 .env 或生产密钥管理系统中。
database:
  host: "127.0.0.1"
  port: 3306
  name: "agent_business_demo"
  user: "root"
  connectionLimit: 10


businessRule:
  # 金额达到该值时创建人工审核任务。
  amountAuditThreshold: 1000


log:
  # 可选值：debug / info / warn / error
  level: info
